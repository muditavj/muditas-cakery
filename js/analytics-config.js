/* Analytics endpoint — the deployed Cloudflare Worker's URL, no trailing slash.
   While this is empty, tracking is off and the Admin → Analytics tab explains why. */
window.CAKERY_ANALYTICS = { endpoint: "https://cakery-analytics.muditavijay06.workers.dev" };
